# HANDOFF · 프로그램 등급(level) 삭제 + 남은 항목 정리

- **시작**: 2026-08-23 (KST)
- **지시 원문**: "프로그램 등급은 삭제해줘 / 콘텐츠는 차후에 내가 올릴께 /
  운영 메일은 그냥 그대로 유지할꺼야"
- **상태**: 완료 (2026-08-23) · v01.098.00

## 해석
1. **등급 삭제** — 프로그램의 `level`(Beginner · Intermediate · All Levels) 제거.
   데이터 + 공개 화면 + 관리자 입력 + 구조화 데이터(`Course.educationalLevel`)까지.
2. **콘텐츠** — 뉴스·파트너·이미지·장학은 운영자가 직접 올린다 → 내 대기 목록에서 내린다.
   (빈 상태 안내는 이미 들어가 있어 그대로 두면 된다)
3. **운영 메일** — `hello@` 포함 현행 유지 → 대기 목록에서 내린다.

## 진행
- [x] `level` 사용처 전수 → 데이터(KV 5 + 코드 기본값 10) · 공개 화면 2곳 · 관리자 4곳 · 구조화 데이터 1곳
- [x] 전부 제거 · v01.098.00 배포 · 검증

## 등급(level) 제거 범위
| 자리 | 내용 |
|---|---|
| KV `programs[].level` ×5 | 삭제 |
| KV `program_detail.label_level` ko/en | 삭제 |
| `content-store.js` 기본 프로그램 5개 + 라벨 | 삭제 |
| `worker.js` `CURRENT_PROGRAMS` 5개 | 삭제 |
| `Programs.jsx` | **등급 필터 칩 UI + 필터링 로직 통째 제거**(카테고리 필터는 유지) |
| `ProgramDetail.jsx` | 히어로 메타 배지 + 스펙 표 행 제거 |
| `admin.html` | 목록 열 · 필터 드롭다운 · 입력 필드 · 상태값 제거 |
| `worker.js` `seoCourse()` | `educationalLevel` 제거(구조화 데이터) |

## 🔴 하는 김에 잡은 것 — 코드 기본값에 CUFS 가 그대로 남아 있었다
- 8/22 에 KV 의 CUFS 를 정리했지만 **코드 기본값(`DEFAULT_CONTENT`)에는 33곳이 남아 있었다.**
  `worker.js` 46곳, `admin.html` 5곳도 마찬가지.
- **KV 가 초기화되거나 원격 콘텐츠 도착 전 첫 페인트에서 전부 되살아난다.**
  전날 상단 배너에서 똑같이 당한 함정이다(실패 기록 등재분).
- 정리: 프로그램 kicker · 섹션 제목 · 히어로 · 소개 문구 · 안내 메일 2종(외부 입시 링크 포함) ·
  제3자 동의서 예시 · 기본 FAQ · 파트너 기본 카드 · 프로그램 상세 기본 본문
  (`government-recognized CUFS micro-degree certificate` 같은 **기관 귀속 주장** 포함) ·
  관리자 신규 프로그램 템플릿·라벨.
- **결과: 코드 전체 CUFS 0곳.** (남은 1곳은 KV 의 개인 경력 기록, 의도적 유지)

## 나머지 지시 반영
- **콘텐츠(뉴스·파트너·이미지·장학)** → 운영자가 직접 올린다. 내 대기 목록에서 내림.
  빈 상태 안내는 이미 들어가 있어 그대로 두면 된다.
- **운영 메일(`hello@` 포함)** → 현행 유지. 대기 목록에서 내림.

## 검증
- 관리자 콘솔 정상 로드(4xx 0 · 예외 0), 공개 사이트 전 라우트 4xx 0 · 예외 0.
- KV·코드 양쪽에서 `level` 제거 확인, 코드 CUFS 0.

# HANDOFF · 운영자 지시 6건 라운드

- **시작**: 2026-08-27 (KST)
- **상태**: 완료 (v01.101.05 배포·푸시·위키 반영)
- **기준 버전**: v01.101.03

## 지시 목록 (운영자, 2026-08-27)
1. **오류 71건** — 최근 100 이벤트 중 71건이 오류. 전 원인 분석·해결.
2. **방문자 분석에서 AI 에이전트 제외** — Claude · ChatGPT · Grok 접속은 카운팅 제외.
3. **대시보드 접기/펼치기 제거** — 개념 자체를 없애고 전부 펼친 상태로.
4. **이모지 전면 금지** — 규칙·기억 단에서 강하게 막고, 사이트에서 톤앤매너에
   맞지 않는 이모지는 전부 제거하거나 대체.
5. **관리자 인사이트 챗봇** — 우측 하단. 운영 데이터 조회, **규칙 기반(LLM 없음)**
   으로 확정(운영자 선택, 2026-08-27).
6. **핸드북 최신화** — 위 결과를 한 번에 반영.

## 진행
- [x] ① **오류 71건** — 장애가 아니라 집계 결함이었다. 최근 25건 중 22건이
      bingbot 한 마리, 나머지는 이미 고쳐진 옛 오류인데 해결 표시가 없었다.
      메일 수신 성공 로그(`info`)까지 오류로 세고 있었다. 원인별로 사유를 적어
      `resolved` 처리(지우지 않았다) + `level=error,warn` 로 카운트 교정.
- [x] ② **봇 제외** — `isBotUserAgent()` + 마이그레이션 0041(`is_bot`).
      분석 읽기 10곳 + `/api/errors` POST. 패턴이 둘인 이유: AI 페처는
      스스로 bot 이라 밝히지 않는다.
- [x] ③ **접기 제거** — `<details>` 76개 상시 `open` + 토글 무력화.
- [x] ④ **이모지 제거** — UI·메일 기본값·라이브 KV. preflight 검사 추가로
      재발 차단. 그 검사가 손 스캔이 놓친 `✉️` 를 잡았다.
- [x] ⑤ **인사이트 콘솔** — `POST /api/admin/insight` + 우측 하단 패널.
      규칙 기반(LLM 없음, 운영자 선택). 15케이스 시험 통과.
- [x] ⑥ **KMS 최신화** — 두 달 낡은 7. 이슈 목록을 코드와 대조해 재작성,
      6. 관리자 화면 규약 추가, 번호 충돌(7이 둘) 해소.

## 다음으로 넘긴 것
- `admin.html` 인라인 스크립트 500KB 초과 — Babel 최적화 포기 경고. 분할 후보.
- 배포 전 `.jsx` 자동 구문검사가 **없다**. 구문 오류 하나면 SPA 전체가 죽는다.
- 수신 메일 `Reply-To` 미존중 · envelope sender 미보존 (마이그레이션 필요).

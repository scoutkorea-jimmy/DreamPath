# HANDOFF · 구조 정리 (헬퍼 · 죽은 콘텐츠 키 · 라우팅)

- **시작**: 2026-08-27 (KST)
- **지시**: "라우팅까지 모두 진행하" (구조 점검 보고에 대한 응답)
- **상태**: 진행 중
- **기준 버전**: v01.101.06

## 점검에서 나온 것 (실측)
- `handleApi` 단일 함수 **2,839줄** = `worker.js` 의 37%. 라우트 분기 69개 중
  **14개가 51줄 초과 인라인 로직**(최대 301줄).
- `admin.html` 에 공통 fetch 헬퍼가 **없다** — 토큰 읽기 40회 · `Bearer` 조립
  41회 · `fetch` 46회가 복사돼 있다.
- `isAdmin` 검사 한 줄이 **55곳** 복사. 지금은 구멍이 없지만
  (`rules/04-history-failure.md`) "권한은 한 곳에서 잠근다"와 같은 형태다.
- `partners_section` / `stories_section` — 관리자에서 **편집은 되는데 렌더가
  안 된다.** 운영자가 고쳐도 화면이 안 바뀐다.
- 죽은 CSS 클래스 40개 · 죽은 함수 2개(`parseEmailAddress` · `requireRole`).

## 범위 (운영자 지시: "라우팅까지")
1. [ ] 관리자 fetch 헬퍼 하나로 통합
2. [ ] `partners_section` / `stories_section` 처리
3. [ ] `handleApi` 의 51줄 초과 분기 14개를 함수로 추출
4. [ ] 죽은 함수 2개 제거
- **제외**: `admin.html` 파일 분할(④) — 회귀 위험이 커 별도 라운드.

## 안전 장치
- 매 단계 `node rules/tools/preflight.mjs` (JSX 구문검사 포함)
- 라우팅 추출은 **동작을 바꾸지 않는 순수 이동**이어야 한다. 추출 전후로
  라우트 목록·응답을 대조한다.

## 진행
- [ ] **← 지금 여기** 1. fetch 헬퍼

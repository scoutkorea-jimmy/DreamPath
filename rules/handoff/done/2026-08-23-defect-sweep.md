# HANDOFF · 결함·모순 전수 수정

- **시작**: 2026-08-23 (KST)
- **지시 원문**: "결함들 모두 수정하고 모순들도 모두 수정해"
- **상태**: 완료 (2026-08-23) · v01.097.05

## 접근
추측으로 훑지 않는다. 근거는 셋:
1. **D1 `error_logs`** — 실제로 터진 것
2. **전 라우트 브라우저 크롤** — 예외·빈 화면·죽은 링크
3. **상태 대조** — 프로그램 비공개 / 신규 모집 없음 이라는 현재 사실과 어긋나는 문구·링크·기계 데이터

## 진행
- [x] D1 `error_logs` 전수 — 실제로 터진 것 확인
- [x] 전 14개 라우트 브라우저 크롤 (예외·4xx·빈 화면·죽은 링크)
- [x] 상태 대조(프로그램 비공개 / 신규 모집 없음) — 어긋나는 문구·링크·기계 데이터
- [x] 수정 + 재크롤 검증 · v01.097.05 배포

## 오류 로그에서 나온 것 (근거: D1 `error_logs`)
| 건수 | 내용 | 판정 |
|---:|---|---|
| 22 | `Could not load Nav.jsx` (부팅 중 파일 잘림) | **다른 세션이 부트 워치독으로 처리 중** |
| 10 | TipTap esm.sh 모듈 파싱 실패 | 다른 세션이 처리 중(editor-loader) |
| 7 | `/apply` `lang is not defined` (2026-05) | **현재 코드에 없음** — `Apply({lang})` 로 이미 해결 |
| 7 | `/api/me/notifications` D1 타임아웃 | 인프라 일시 장애(코드 결함 아님) |
| 4 | `structuredClone is not defined` | **이미 폴리필됨**(content-store) |
| 1 | `/offline` React error #130 | `safe()` 가드 + `window.ErrorOffline` 존재 — 현재 재현 안 됨 |

→ **로그의 나머지는 과거분이거나 이미 고쳐진 것.** 지금 살아 있는 결함은 크롤에서 나왔다.

## 크롤에서 나온 결함 (근거: 실제 브라우저)
1. **`/api/auth/me` 가 익명에게 401** — **전 14개 라우트 14/14** 에서 콘솔 빨간 오류.
   보호 자원이 아니라 "지금 누구냐" 조회다. 로그인 안 된 상태는 오류가 아니라 사실.
   → `200 { user: null }`. 클라이언트도 함께 수정(배포 시차 + 무효 레거시 토큰 무한 재시도 방지).
2. **`/partners` 본문 통째로 비어 있음** — 파트너 0곳인데 빈 상태 안내가 없어 히어로 아래가 뚝 끊겼다.
   → `/news` 처럼 안내 문구 추가.

## 상태와 어긋나던 것 (모순)
| 무엇 | 어떻게 |
|---|---|
| 히어로 "Explore programs" → 안내 화면 | 프로그램 비공개면 **버튼 숨김**, 소개 버튼을 주 버튼으로 |
| 하단 CTA "학습하러 가기" → 모집 없는 `/apply` | 모집 없으면 **"문의하기"**로 |
| 상단 메뉴 "프로그램" 아래에 소식·후기만 | 라벨을 **"소식·후기"**로 |
| `sitemap` `/apply` 우선순위 0.9 | 모집 없는 동안 **0.4** |
| `how` 2단계 "마이크로디그리, **온라인 학위**, 어학 트랙 중 선택" | 공개된 것이 하나도 없는 카탈로그 주장 → **"모집이 열리면 안내된 프로그램 중에서 선택"** |

모두 **스위치를 풀면 자동 복귀**하도록 코드에서 분기했다(문구를 지워 없애지 않았다).

## 검증
- 재크롤: **HTTP 4xx 0건**(이전 14/14 401), JS 예외 0건, `/partners` 안내 노출,
  히어로 버튼 1개, CTA "Talk to us", 메뉴 "News & stories".
- `/api/auth/me` 익명·무효토큰 모두 `200 {user:null}`.
- 관리자 콘솔 정상 로드(4xx 0 · 예외 0), FAQ 렌더 57개 요소, 챗봇 정상.

## 남은 것 — **사실이 필요해 내가 못 고치는 것**
| # | 항목 | 필요한 것 |
|---|---|---|
| 1 | 프로그램 등록금 5종 임시값 `$500` | 실제 금액 (재공개 전 필수) |
| 2 | 브랜드 표기 혼재 (`Dream Path` 54 / `KoreaDreamPath` 58) | 본문 정본 1개 결정 → 일괄 치환 |
| 3 | 뉴스 0건 (`/news` 빈 페이지, 메뉴에 남음) | 글 작성 or 메뉴에서 제외 |
| 4 | 파트너 0곳 | 공개 가능한 기관 목록 |
| 5 | 히어로 배경 5개 · OG 공유 이미지 없음 | 이미지 |
| 6 | 장학 3건 (2026-06 등록) | 최신 회차 |
| 7 | `hello@` 메일함 | 유지/정리 |
| 8 | Cloudflare 목적지 인증 (kakao 포워드) | 인증 링크 클릭 |
| 9 | 다른 세션 동시 작업 | 병행 여부 알려주기 |
